# TConstruct -- Access Transformer
# Phase 0: intentionally empty. Real AT entries (NeoForge 1.21.1 syntax,
# Mojang-mapped names) are reintroduced in later phases alongside the code that needs them.
